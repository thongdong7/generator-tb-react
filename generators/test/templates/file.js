/**
 * Created by hiepsimu on 6/25/16.
 */
import { expect, assert } from 'chai';

describe('<%= group %>', () => {
  describe('<%= name %>', () => {
    it('', () => {
      assert.equal(1, 2)
    })
  })
})
